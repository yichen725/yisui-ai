# 逸碎 AI 后端部署指南（Railway 免费版）

## 一、准备工作

你需要准备以下文件（已包含在交付包中）：
- `server.js` - 后端主程序
- `package.json` - 项目配置
- `cards.json` - 卡密数据（含 3 个测试卡密）

你还需要：
- 一个 GitHub 账号（用于授权 Railway 访问代码）
- 一个 DeepSeek 或 OpenAI 的 API Key（用于后端转发聊天请求）

---

## 二、部署后端到 Railway（约 10 分钟）

### 步骤 1：注册 Railway 账号

1. 打开浏览器，访问 https://railway.app
2. 点击右上角的 **"Sign Up"** 按钮
3. 选择 **"Continue with GitHub"**（用 GitHub 账号登录，最方便）
4. 在 GitHub 授权页面点击绿色的 **"Authorize railwayapp"** 按钮
5. 注册成功后，你会获得 $5 的免费额度（无需绑定信用卡）

### 步骤 2：创建新项目

1. 登录后进入 Railway Dashboard，点击右上角的 **"+ New Project"** 按钮
2. 在弹出的菜单中选择 **"Deploy from GitHub repo"**
3. 第一次使用需要授权 Railway 访问你的 GitHub 仓库：
   - 点击 **"Configure GitHub App"**
   - 选择你的 GitHub 账号
   - 选择 **"All repositories"**（或只选择你要部署的仓库）
   - 点击 **"Install"** 按钮
4. 授权完成后，回到 Railway，你会看到你的 GitHub 仓库列表
5. 选择包含后端代码的仓库（比如 `yisui-ai`）
6. Railway 会自动检测到这是一个 Node.js 项目

### 步骤 3：配置环境变量

**这一步非常重要！** 不配置 API Key，聊天功能无法使用。

1. 在项目部署页面，点击 **"Variables"** 标签页（或 "Settings" → "Variables"）
2. 点击 **"New Variable"** 按钮，添加以下环境变量：

| 变量名 | 值 | 说明 |
|--------|-----|------|
| `OPENAI_KEY` | `你的DeepSeek或OpenAI的API Key` | 例如 `sk-xxxxxxxxxx` |
| `TARGET_API_URL` | `https://api.deepseek.com/v1/chat/completions` | 可选，默认就是 DeepSeek 地址 |

3. 点击 **"Add"** 保存

> **注意**：`OPENAI_KEY` 是你从 DeepSeek 官网（https://platform.deepseek.com）申请的 API Key，不是你自己编的。

### 步骤 4：确认启动命令

1. 点击 **"Settings"** 标签页
2. 找到 **"Start Command"** 选项
3. 确认启动命令是：`node server.js`
   - 如果不是，手动修改为 `node server.js`
4. Railway 会自动运行 `npm install` 安装依赖，无需手动配置

### 步骤 5：等待部署完成

1. 回到 **"Deployments"** 标签页
2. 你会看到部署正在进行中，状态显示为 "Building" 或 "Deploying"
3. 等待约 2-3 分钟，状态变为 **"Success"** 或绿色对勾
4. 点击部署记录，可以查看实时日志

### 步骤 6：获取公网地址

1. 点击 **"Settings"** 标签页
2. 找到 **"Domain"** 部分
3. 你会看到一个类似 `https://yisui-ai-production.up.railway.app` 的地址
4. **复制这个地址**，后面配置前端需要用到

> **验证后端是否正常**：在浏览器访问 `https://你的地址/health`，如果返回 `{"status":"ok",...}` 说明部署成功。

---

## 三、配置前端并重新部署到 GitHub Pages

### 步骤 1：修改前端默认后端地址

1. 打开 `index.html` 文件
2. 找到第 430 行左右的 `defaultConfig`：
   ```javascript
   const defaultConfig = {
       chatProvider: 'custom',
       chatApiUrl: 'https://YOUR_BACKEND_URL/v1/chat/completions',
       ...
   };
   ```
3. 把 `https://YOUR_BACKEND_URL` 替换成你刚才在 Railway 拿到的地址，例如：
   ```javascript
   chatApiUrl: 'https://yisui-ai-production.up.railway.app/v1/chat/completions',
   ```
4. 保存文件

### 步骤 2：上传到 GitHub 仓库

**方法一：网页上传（最简单）**

1. 打开你的 GitHub 仓库页面（比如 `https://github.com/yichen725/yisui-ai`）
2. 点击 **"Add file"** → **"Upload files"**
3. 把修改后的 `index.html` 拖到上传区域
4. 滚动到底部，点击 **"Commit changes"** 按钮
5. GitHub Pages 会自动重新部署，等待约 1-2 分钟

**方法二：命令行上传**

```bash
git add index.html
git commit -m "更新后端地址为 Railway"
git push
```

### 步骤 3：验证前端

1. 访问你的 GitHub Pages 地址：`https://yichen725.github.io/yisui-ai/`
2. 页面应该会弹出卡密验证窗口
3. 输入测试卡密（见下文），点击验证
4. 验证成功后，输入框应该可以正常输入
5. 发送一条消息测试聊天功能是否正常

---

## 四、卡密管理

### 测试卡密（初始包含 3 个）

```
yisui-test-001
yisui-test-002
yisui-test-003
```

### 如何添加新卡密

**方法一：直接编辑 cards.json**

1. 打开 `cards.json` 文件
2. 按照以下格式添加新卡密：
   ```json
   [
     { "key": "yisui-test-001", "used": false },
     { "key": "你的新卡密", "used": false }
   ]
   ```
3. 保存文件，提交到 GitHub，Railway 会自动重新部署

**方法二：用命令行快速生成卡密**

在本地运行以下命令生成 10 个随机卡密：

```bash
node -e "
const fs = require('fs');
const cards = [];
for(let i=0;i<10;i++){
  const key = 'yisui-' + Math.random().toString(36).substring(2,10);
  cards.push({key, used:false});
}
fs.writeFileSync('cards.json', JSON.stringify(cards, null, 2));
console.log('生成的卡密：');
cards.forEach(c => console.log(c.key));
"
```

### 如何查看已使用的卡密

访问 `https://你的后端地址/health`，会返回：
```json
{
  "status": "ok",
  "apiKeyConfigured": true,
  "totalCards": 3,
  "usedCards": 1,
  "availableCards": 2
}
```

### 卡密使用原理

1. 用户打开网页 → 弹出卡密验证窗口
2. 用户输入卡密 → 前端请求后端 `/verify` 接口
3. 后端在 `cards.json` 中查找该卡密：
   - 如果不存在 → 返回"卡密无效"
   - 如果已使用（`used:true`）→ 返回"该卡密已被使用"
   - 如果未使用 → 标记为已使用，返回"验证成功"
4. 前端把验证状态存入 localStorage，用户下次打开无需再次验证

> **注意**：卡密是一次性的，验证成功后就不能再用了。每个卡密绑定一个浏览器（localStorage），用户清除浏览器数据后需要重新验证。

---

## 五、常见问题排查

### Q1：后端部署失败怎么办？

1. 点击 Railway 的部署记录，查看错误日志
2. 常见错误：
   - `Cannot find module 'express'` → 确认 `package.json` 中有 express 依赖
   - `PORT` 相关错误 → Railway 会自动设置 PORT，不要在代码中写死端口
   - 构建超时 → 检查代码是否有语法错误

### Q2：聊天功能没反应？

1. 打开浏览器开发者工具（F12）→ Console 标签页
2. 查看是否有报错
3. 常见原因：
   - 后端地址填错了 → 检查 `defaultConfig.chatApiUrl`
   - 后端没配置 `OPENAI_KEY` → 检查 Railway 环境变量
   - API Key 无效或余额不足 → 检查 DeepSeek 账号余额

### Q3：卡密验证一直失败？

1. 确认后端地址正确
2. 访问 `https://你的后端地址/health`，确认后端正常运行
3. 确认 `cards.json` 中有未使用的卡密
4. 卡密区分大小写，注意不要输入空格

### Q4：Railway 免费额度用完了怎么办？

- Railway 免费版提供 $5 额度，按实际使用量计费
- 一个轻量的 Node.js 服务每月大约消耗 $2-$3
- 额度用完后服务会停止，可以升级到付费计划或换其他平台

---

## 六、文件清单

交付包包含以下文件：

| 文件名 | 说明 |
|--------|------|
| `server.js` | 后端主程序（聊天代理 + 卡密验证） |
| `package.json` | Node.js 项目配置 |
| `cards.json` | 卡密数据（3 个测试卡密） |
| `index.html` | 修改后的前端（已加入卡密验证） |
| `部署指南.md` | 本部署指南 |

---

## 七、快速检查清单

部署完成后，按以下顺序检查：

- [ ] 后端地址访问 `/health` 返回 `{"status":"ok"}`
- [ ] Railway 环境变量中已配置 `OPENAI_KEY`
- [ ] 前端 `defaultConfig.chatApiUrl` 已替换为实际后端地址
- [ ] 前端页面打开后弹出卡密验证窗口
- [ ] 输入测试卡密验证成功
- [ ] 发送聊天消息能正常收到回复
- [ ] 已使用的卡密再次验证会提示"已被使用"

全部勾选就说明部署成功了！
